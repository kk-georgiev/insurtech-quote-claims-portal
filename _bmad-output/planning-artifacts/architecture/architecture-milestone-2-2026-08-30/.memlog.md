---
scope: Milestone 2 design system: Tailwind adoption, design tokens, base component library
purpose: build-substrate
altitude: feature
updated: 2026-08-30T16:08
---

- (constraint) Inherited from parent spine (architecture-motor-insurance-quote-claims-portal-2026-08-23, AD-10): React Router v8 owns routing, one role-guard wrapper component, one typed fetch client, no data-fetching library - binding, unaffected by this milestone
- (constraint) Inherited from parent spine (AD-8): i18n is 100% frontend-owned via react-i18next, keys namespaced per feature - Tailwind adoption must not touch or duplicate this mechanism
- (constraint) No prior AD in the parent spine governs CSS/styling approach - the Structural Seed lists frontend/src/{app,features,i18n,api} with no styling directory or convention; current reality per README is 'minimal global styling, no framework' - this is genuinely new ground for this spine to cover
- (decision) PRD (Milestone 2, finalized 2026-08-30) already settled: Tailwind CSS as styling approach, Inter as typeface (open substitute for Sirma's proprietary TT Norms Pro), navy #2A2859 + restrained accent as the token base, and a 4-component base library (Button, Input, FormField, Card) - these are [ADOPTED] inputs to this spine, not open questions
- (version) Verified via web (2026-08-30): Tailwind CSS latest stable is v4.3.3 (npm). v4 is CSS-first - no tailwind.config.js by default, uses @theme directive directly in CSS. Vite integration via @tailwindcss/vite plugin (npm install tailwindcss @tailwindcss/vite), added to vite.config.ts plugins array. Sources: tailwindcss.com/docs, tailwindcss.com/blog/tailwindcss-v4, npmjs.com/package/tailwindcss
- (version) Verified via web (2026-08-30): class-variance-authority (cva) latest npm version is 0.7.1 (stable, mature utility - Tailwind-version-agnostic, only composes className strings, unaffected by Tailwind v4's CSS-first config change). Source: npmjs.com/package/class-variance-authority, cva.style
- (decision) Coaching decisions confirmed: (1) cva for component variants over plain conditional className - cheap insurance against divergent patterns as the library grows across milestones; (2) frontend/src/components/ui/ as the component library location over design-system/ - more conventional/discoverable; (3) enforcement via convention + code review only, no ESLint rule - proportionate for a 2-person team, risk accepted explicitly rather than left silent
- (version) Verified via web (2026-08-30): tailwind-merge latest npm version is 3.6.0. clsx + tailwind-merge combined via a cn() helper is the standard companion pattern to cva in the Tailwind ecosystem (resolves conflicting utility classes when a component's own classes and a caller-passed className prop overlap) - adopted alongside cva rather than as a separate coaching round, since it's the well-known default that comes with the cva choice.
- (decision) AD drafting: AD-1 Tailwind v4 CSS-first tokens, AD-2 cva+clsx+tailwind-merge for component variants in frontend/src/components/ui/, AD-3 components render native semantic elements (protects existing role-based test queries), AD-4 no automated lint enforcement - convention+review only
- (event) Reviewer gate: version-check PASS (minor gap: @tailwindcss/vite peer range vs clsx version not explicitly cited); adversarial CONCERNS with 5 real divergence pairs found (token naming grammar, className override boundary ambiguity, field-error state ownership split, missing status-color vocabulary, Card API shape). All 5 are cheap to close with tightened/new ADs, none require PRD scope changes - applying fixes now.
- (decision) Applied all 5 adversarial-review fixes: tightened AD-1 (semantic token naming), tightened AD-2 (className boundary = spacing/sizing/positioning only, Card flat-props API), added AD-5 (FormField owns error message, Input owns invalid boolean), added AD-6 (fixed success/warning/danger/info status vocabulary, no Badge component this milestone). Also closed version-review gaps: cited @tailwindcss/vite peer range vs Vite 8, pinned clsx 2.1.1, noted cva's 2-year dormancy as a watch item. Lint clean after edits.
- (event) spine finalized
