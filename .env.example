POSTGRES_DB=motor_insurance
POSTGRES_USER=motor_insurance
POSTGRES_PASSWORD=local_development_only
POSTGRES_PORT=5432

DB_URL=jdbc:postgresql://localhost:5432/motor_insurance
DB_USERNAME=motor_insurance
DB_PASSWORD=local_development_only

# Leave empty in local development so Vite proxies /api to the backend.
VITE_API_BASE_URL=
VITE_DEV_API_TARGET=http://localhost:8080
